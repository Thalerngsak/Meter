package com.pwa.web.helper;

import javax.servlet.ServletContext;

import com.pwa.data.model.config.Config;
import com.pwa.data.model.config.ConfigDAO;

public class ServletContextHelper {
	public static void initContext(ServletContext context) {
		//ConfigDAO dao = new ConfigDAO();
		//String id = (String)context.getAttribute("branch");
		//Config c = dao.findById(id);
//		if(c != null){			
//			context.setAttribute("br", c.getBr());
//			context.setAttribute("head", c.getHead());
//			context.setAttribute("telno", c.getTelno());
//			
//		}
		
	}
}
